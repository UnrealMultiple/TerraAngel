--- src/Terraria/Terraria/Cloud.cs
+++ src/TerraAngel/Terraria/Cloud.cs
@@ -252,7 +_,7 @@
         return MemberwiseClone();
     }
 
-    public static void UpdateClouds()
+    public static void UpdateClouds(GameTime gameTime)
     {
         if (Main.netMode == 2)
         {
@@ -264,7 +_,7 @@
         {
             if (Main.cloud[i].active)
             {
-                Main.cloud[i].Update();
+                Main.cloud[i].Update(gameTime);
                 if (!Main.cloud[i].kill)
                 {
                     num++;
@@ -360,7 +_,7 @@
         return num * num2;
     }
 
-    public void Update()
+    public void Update(GameTime gameTime)
     {
         if (WorldGen.drunkWorldGenText && Main.gameMenu)
         {
@@ -414,7 +_,7 @@
             scale -= 0.0001f;
         }
 
-        position.X += Main.windSpeedCurrent * 9f * GetParallax() * (float)Main.dayRate;
+        position.X += Main.windSpeedCurrent * 9f * GetParallax() * (float)Main.dayRate * 60f * (float)gameTime.ElapsedGameTime.TotalSeconds;
         float num = 600f;
         if (Main.bgAlphaFrontLayer[4] == 1f && position.Y > 200f)
         {
